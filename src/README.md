# src folder (the model layer)

This is the Model layer. This folder supports the Principle of Encapsulation because it isolates the PascalCase PHP classes as they contain all the logic and database connections.

TL;DR: it follows the **PSR-4** standard for class structure and **Encapsulation**.
