# assets folder (presentation layer)

This folder isolates the CSS (style) and JavaScript (behavior) files from the HTML/PHP (structure) files to make it easier to maintain and scale.

TL;DR: It supports the **Separation of Concerns (SOC)**
